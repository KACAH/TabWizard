package datastruct;

/**
 * The TWPercussionTrack class represents only percussions song's track.
 * 
 * 
 */
public class TWPercussionTrack extends TWTrack {
	
	/**
	 * Create track with default settings. Is good for testing or if need to change only a few settings
	 */
	TWPercussionTrack(TWSongPart parentPart, TWTrackHeader header) {
		super(parentPart, header);
	}
}
